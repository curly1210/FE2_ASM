// import { useQuery } from "@tanstack/react-query";
// import { getListCart } from "../provider/dataProvider";

// type useListProps = {
//   resource: string;
//   idUser: number | undefined;
// };

// const useListCart = ({ resource, idUser }: useListProps) => {
//   return useQuery({
//     queryKey: [resource],
//     queryFn: () => getListCart({ resource, idUser }),
//   });
// };

// export default useListCart;
